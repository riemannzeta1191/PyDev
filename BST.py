class BST(object):
    def __init__(self,root,size=None):
        self._root = root
        self._size = size

    class Node(object):
        def __init__(self,val):

            self._val  = val

    def _insertNode_(self,root,size,Node):
        if self.root is None:
            newNode = Node(Node.val)
        elif self.Node._val > self.Node.    
		
		
	//Some comments dude lets hangout